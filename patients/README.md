# Patient bundle directory

Drop per-patient JSON files here, named `<patientId>.json`.

Each file is the output of the EMR's **Export for Portal** action.

Example: a patient whose ID is `12345` lives at `patients/12345.json`.
